import {useState} from 'react';
import {useMutation} from '@tanstack/react-query';
import {Link} from 'react-router-dom';
import {timeLabel,type AttendanceDto,type AttendanceStatus} from '../../../../packages/shared/src/index';
import {api,json,queryClient} from '../api';
import {Avatar,ErrorBox,StatusBadge,StatusSelector,Empty} from './ui';
export function AgentStatusCard({row,date,editable=true}:{row:AttendanceDto;date:string;editable?:boolean}){const [optimistic,setOptimistic]=useState<AttendanceStatus|null>(null);const mutation=useMutation({mutationFn:(status:AttendanceStatus)=>api('/attendance/'+row.agentId,json('PATCH',{status,date})),onMutate:status=>setOptimistic(status),onError:()=>setOptimistic(null),onSuccess:async()=>{await queryClient.invalidateQueries({queryKey:['attendance']});await queryClient.invalidateQueries({queryKey:['dashboard']});setOptimistic(null);}});return <article className="agent-card"><div className="agent-identity"><Avatar name={row.agent.fullName} url={row.agent.avatarUrl}/><div><h3>{row.agent.fullName}</h3><p className="muted">{row.agent.jobTitle}</p></div></div><div className="agent-status">{editable?<StatusSelector name={row.agent.fullName} status={optimistic??row.currentStatus} disabled={mutation.isPending} onSelect={s=>mutation.mutate(s)}/>:<StatusBadge status={row.currentStatus}/>}<small className="muted">{mutation.isPending?'Saving…':row.lastUpdatedAt?'Updated '+timeLabel(row.lastUpdatedAt):'Awaiting update'}</small></div>{mutation.error&&<ErrorBox error={mutation.error} retry={()=>mutation.mutate(mutation.variables!)}/>}</article>;}
export function AttendanceTable({rows,date,editable=false,history=false}:{rows:AttendanceDto[];date:string;editable?:boolean;history?:boolean}){if(!rows.length)return <Empty>No agents match your filters.</Empty>;return <div className="attendance-table"><div className="table-heading"><span>AGENT</span><span>{history?'DATE':'TEAM LEADER'}</span><span>STATUS</span><span>UPDATED</span><span>{history?'DETAILS':'UPDATED BY'}</span></div>{rows.map(row=><AttendanceTableRow key={row.id} row={row} date={date} editable={editable} history={history}/>)}</div>;}
function AttendanceTableRow({row,date,editable,history}:{row:AttendanceDto;date:string;editable:boolean;history:boolean}){const mutation=useMutation({mutationFn:(status:AttendanceStatus)=>api('/attendance/'+row.agentId,json('PATCH',{status,date})),onSuccess:()=>{void queryClient.invalidateQueries({queryKey:['attendance']});void queryClient.invalidateQueries({queryKey:['dashboard']});}});return <div className="table-row"><div className="agent-identity"><Avatar name={row.agent.fullName} url={row.agent.avatarUrl}/><div><strong>{row.agent.fullName}</strong><small>{row.agent.jobTitle}</small></div></div><span className="table-leader">{history?row.attendanceDate.slice(0,10):row.agent.team?.teamLeader?.fullName??'Unassigned'}</span><div>{editable?<StatusSelector name={row.agent.fullName} status={row.currentStatus} disabled={mutation.isPending} onSelect={status=>mutation.mutate(status)}/>:<StatusBadge status={row.currentStatus}/>}</div><small>{timeLabel(row.lastUpdatedAt)}</small>{history?<Link className="text-link" to={`/admin/history/${row.agentId}?date=${row.attendanceDate.slice(0,10)}`}>View audit →</Link>:<small>{row.lastUpdatedBy?.fullName??'—'}</small>}{mutation.error&&<ErrorBox error={mutation.error}/>}</div>;}
